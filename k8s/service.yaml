apiVersion: v1
kind: Service
metadata:
  name: echo-server
  labels:
    name: echo-server
    role: server
spec:
  type: NodePort
  ports:
    - port: 8880
      name: http
      targetPort: 8080
  selector:
    name: echo-server
